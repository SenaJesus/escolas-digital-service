# Server
PORT=8001
NODE_ENV=development

# PostgreSQL
DB_HOST=localhost
DB_PORT=5433
DB_NAME=escolas_org
DB_USER=escolas
DB_PASSWORD=escolas

# JWT
JWT_SECRET=change-me-in-production
JWT_EXPIRATION_MINUTES=30

# SMTP (deixe SMTP_USER vazio para usar inbox fake em modo dev)
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
EMAIL_FROM=escolas.digital@example.com

# CORS
CORS_ORIGIN=*
